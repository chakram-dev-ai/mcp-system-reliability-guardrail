# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# Test + coverage artefacts
.coverage
.coverage.*
htmlcov/
.pytest_cache/
.tox/

# Monitor output. This is EVIDENCE -- archive it off-box before deleting.
# It is ignored here only so a local test run cannot commit an event log.
events.jsonl
*.jsonl
.gm-canaries/
.gm-canaries-escape/

# Local operator config, which will contain real paths and account names
policy.local.yaml
.claude/settings.local.json

# Editors / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db
